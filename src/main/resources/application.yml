server:
  port: ${PORT:8080}

spring:
  thymeleaf: 
    reactive:
      max-chunk-size: 8192
    cache: false

      
---
spring:
  profiles: docker
  data:
    mongodb:
      uri: mongodb://mongodb:27017/reactivecompany

